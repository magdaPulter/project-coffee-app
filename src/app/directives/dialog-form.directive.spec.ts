import { DialogFormDirective } from './dialog-form.directive';

describe('DialogFormDirective', () => {
  it('should create an instance', () => {
    const directive = new DialogFormDirective();
    expect(directive).toBeTruthy();
  });
});
